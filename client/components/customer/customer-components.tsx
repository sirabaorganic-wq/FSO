'use client'

import Image from 'next/image'
import Link from 'next/link'
import { useState } from 'react'
import { ArrowRight, Check, Minus, Plus, ShieldCheck, Trash2 } from 'lucide-react'
import type { Address, CartLine, CustomerUser, Order, Recommendation } from '@/types/customer'
import { cartLines, customerUser, recommendations } from '@/data/customer'

export function AuthCard({ mode }: { mode: 'login' | 'register' | 'forgot' | 'otp' }) {
  const [showPassword, setShowPassword] = useState(false)
  const [submitted, setSubmitted] = useState(false)
  const titles = { login: 'Welcome back', register: 'Create your account', forgot: 'Reset your password', otp: 'Check your inbox' }
  const descriptions = { login: 'Your pantry, stories, and saved discoveries are waiting.', register: 'Keep your favourite ingredients close and your orders in one calm place.', forgot: 'Enter your email and we will send a secure reset link.', otp: 'We sent a six-digit code to ananya.rao@example.com.' }
  return <main className="container-shell flex min-h-[calc(100vh-5rem)] items-center justify-center py-20"><div className="grid w-full max-w-5xl overflow-hidden border border-border bg-surface shadow-[var(--shadow-soft)] md:grid-cols-[0.9fr_1.1fr]"><div className="hidden min-h-[560px] bg-primary p-12 text-primary-foreground md:flex md:flex-col md:justify-between"><div><p className="eyebrow text-accent">Flash Sales Online</p><h1 className="display mt-6 max-w-sm text-6xl">Good food begins with knowing where it came from.</h1></div><p className="max-w-xs text-sm leading-6 opacity-75">A considered pantry for people who care about the hands, places, and methods behind what they eat.</p></div><div className="p-6 sm:p-12"><Link href="/" className="text-sm text-muted-foreground hover:text-secondary">← Back to the pantry</Link><div className="mt-14 max-w-md"><p className="eyebrow">{mode === 'otp' ? 'Verification' : 'Your account'}</p><h2 className="display mt-3 text-5xl">{titles[mode]}</h2><p className="mt-4 text-sm leading-6 text-muted-foreground">{descriptions[mode]}</p>{submitted ? <div className="mt-10 border border-success/40 bg-success/10 p-5 text-sm leading-6"><Check className="mb-3 text-success" /><strong>All set.</strong> This is a frontend preview, so no account details were sent.</div> : <form className="mt-8 space-y-5" onSubmit={(event) => { event.preventDefault(); setSubmitted(true) }}>{mode === 'otp' ? <div><label className="text-xs font-bold uppercase tracking-wider" htmlFor="otp">Six-digit code</label><input id="otp" inputMode="numeric" maxLength={6} className="mt-2 min-h-14 w-full border border-input bg-background px-4 text-center text-xl tracking-[0.5em]" placeholder="000000" /></div> : mode !== 'forgot' ? <><Field label="Email address" type="email" placeholder="you@example.com" /><div><label className="text-xs font-bold uppercase tracking-wider" htmlFor="password">Password</label><div className="relative mt-2"><input id="password" type={showPassword ? 'text' : 'password'} className="min-h-12 w-full border border-input bg-background px-4 pr-16" placeholder="Your password" /><button type="button" onClick={() => setShowPassword((value) => !value)} className="absolute right-2 top-1/2 -translate-y-1/2 px-3 text-xs text-muted-foreground">{showPassword ? 'Hide' : 'Show'}</button></div></div>{mode === 'login' ? <label className="flex items-center gap-2 text-sm"><input type="checkbox" /> Remember me</label> : <Field label="Confirm password" type="password" placeholder="Repeat your password" />}</> : <Field label="Email address" type="email" placeholder="you@example.com" />}<button className="flex min-h-12 w-full items-center justify-center gap-2 bg-primary px-5 text-sm font-bold text-primary-foreground hover:bg-secondary">{mode === 'login' ? 'Sign in' : mode === 'register' ? 'Create account' : 'Send secure link'} <ArrowRight size={16} /></button>{mode === 'login' ? <Link href="/auth/forgot-password" className="block text-center text-sm text-muted-foreground hover:text-secondary">Forgot password?</Link> : null}</form>}<div className="mt-10 border-t border-border pt-6 text-center text-sm text-muted-foreground">{mode === 'login' ? <>New here? <Link href="/auth/register" className="font-bold text-secondary">Create an account</Link></> : <>Already have an account? <Link href="/auth/login" className="font-bold text-secondary">Sign in</Link></>}</div></div></div></div></main>
}
function Field({ label, type, placeholder }: { label: string; type: string; placeholder: string }) { return <div><label className="text-xs font-bold uppercase tracking-wider" htmlFor={label}>{label}</label><input id={label} type={type} required className="mt-2 min-h-12 w-full border border-input bg-background px-4" placeholder={placeholder} /></div> }

export function AccountShell({ children, current }: { children: React.ReactNode; current: string }) { const links = [['Overview', '/account'], ['Profile', '/account/profile'], ['Orders', '/account/orders'], ['Wishlist', '/account/wishlist'], ['Addresses', '/account/addresses'], ['Notifications', '/account/notifications'], ['Settings', '/account/settings']]; return <main className="container-shell py-32"><div className="mb-12 flex flex-col justify-between gap-5 border-b border-border pb-8 md:flex-row md:items-end"><div><p className="eyebrow">Your account</p><h1 className="display mt-3 text-6xl">Hello, {customerUser.name.split(' ')[0]}.</h1></div><Link href="/shop" className="inline-flex min-h-11 items-center gap-2 border border-border px-4 text-sm font-bold hover:border-secondary hover:text-secondary">Continue shopping <ArrowRight size={16} /></Link></div><div className="grid gap-12 lg:grid-cols-[220px_1fr]"><aside><nav aria-label="Account navigation" className="flex gap-1 overflow-x-auto lg:flex-col">{links.map(([label, href]) => <Link key={label} href={href} className={`whitespace-nowrap border-b-2 px-3 py-3 text-sm ${current === label ? 'border-secondary font-bold text-secondary' : 'border-transparent text-muted-foreground hover:text-foreground'}`}>{label}</Link>)}</nav></aside><div>{children}</div></div></main> }

export function ProfileCard({ user = customerUser }: { user?: CustomerUser }) { return <div className="flex flex-col gap-5 border border-border bg-surface p-6 sm:flex-row sm:items-center"><div className="relative size-20 shrink-0 overflow-hidden rounded-full bg-surface-muted"><Image src={user.avatar} alt="" fill sizes="80px" className="object-cover" /></div><div><p className="text-xl font-semibold">{user.name}</p><p className="mt-1 text-sm text-muted-foreground">{user.email}</p><p className="mt-2 text-xs font-bold uppercase tracking-wider text-secondary">{user.diet} pantry</p></div><Link href="/account/profile" className="sm:ml-auto text-sm font-bold text-secondary">Edit profile</Link></div> }
export function AddressCard({ address, editable = false }: { address: Address; editable?: boolean }) { return <div className="border border-border bg-surface p-5"><div className="flex items-start justify-between"><div><p className="font-bold">{address.label} {address.isDefault ? <span className="ml-2 text-xs font-normal text-secondary">Default</span> : null}</p><p className="mt-3 text-sm leading-6 text-muted-foreground">{address.name}<br />{address.lines.map((line) => <span key={line}>{line}<br /></span>)}{address.phone}</p></div>{editable ? <button type="button" aria-label={`Edit ${address.label} address`} className="text-sm text-secondary">Edit</button> : null}</div></div> }
export function OrderCard({ order }: { order: Order }) { return <Link href={`/account/order/${order.id}`} className="block border border-border bg-surface p-5 hover:border-secondary"><div className="flex flex-wrap items-center justify-between gap-3"><div><p className="font-bold">{order.id}</p><p className="mt-1 text-xs text-muted-foreground">Placed {order.date} · {order.items.length} items</p></div><span className="border border-success/40 px-3 py-1 text-xs font-bold text-success">{order.status}</span></div><div className="mt-5 flex items-end justify-between border-t border-border pt-4"><p className="text-sm text-muted-foreground">Arrives {order.eta}</p><p className="font-bold">₹{order.total.toLocaleString('en-IN')}</p></div></Link> }
export function RecommendationGrid({ items = recommendations }: { items?: Recommendation[] }) { return <div className="grid gap-5 sm:grid-cols-3">{items.map((item) => <article key={item.slug} className="group"><div className="relative aspect-[4/3] overflow-hidden bg-surface-muted"><Image src={item.image} alt={item.name} fill sizes="(max-width: 640px) 90vw, 30vw" className="object-cover transition-transform duration-300 group-hover:scale-105" /></div><p className="mt-4 text-xs uppercase tracking-wider text-secondary">{item.reason}</p><h3 className="mt-1 font-serif text-2xl">{item.name}</h3><p className="mt-2 font-bold">{item.price}</p></article>)}</div> }
export function CartItem({ line, onChange }: { line: CartLine; onChange: (id: string, quantity: number) => void }) { return <div className="flex gap-4 border-b border-border py-5"><div className="relative size-24 shrink-0 overflow-hidden bg-surface-muted"><Image src={line.image} alt={line.name} fill sizes="96px" className="object-cover" /></div><div className="min-w-0 flex-1"><p className="font-semibold">{line.name}</p><p className="mt-1 text-sm text-muted-foreground">{line.producer} · {line.unit}</p><div className="mt-4 flex items-center gap-3"><div className="flex items-center border border-border"><button type="button" aria-label={`Decrease ${line.name} quantity`} onClick={() => onChange(line.id, Math.max(0, line.quantity - 1))} className="grid size-9 place-items-center"><Minus size={14} /></button><span className="w-8 text-center text-sm">{line.quantity}</span><button type="button" aria-label={`Increase ${line.name} quantity`} onClick={() => onChange(line.id, line.quantity + 1)} className="grid size-9 place-items-center"><Plus size={14} /></button></div><button type="button" onClick={() => onChange(line.id, 0)} className="text-xs text-muted-foreground hover:text-destructive"><Trash2 size={14} /></button></div></div><p className="font-bold">₹{(line.price * line.quantity).toLocaleString('en-IN')}</p></div> }
export function CartSummary({ lines = cartLines, checkout = false }: { lines?: CartLine[]; checkout?: boolean }) { const subtotal = lines.reduce((sum, item) => sum + item.price * item.quantity, 0); const delivery = subtotal >= 999 ? 0 : 80; return <div className="border border-border bg-surface p-6"><p className="eyebrow">{checkout ? 'Your order' : 'Summary'}</p><div className="mt-5 space-y-3 text-sm"><div className="flex justify-between"><span>Subtotal</span><span>₹{subtotal.toLocaleString('en-IN')}</span></div><div className="flex justify-between"><span>Delivery</span><span>{delivery ? `₹${delivery}` : 'Free'}</span></div><div className="flex justify-between"><span>Taxes</span><span>Included</span></div><div className="flex justify-between border-t border-border pt-4 text-base font-bold"><span>Total</span><span>₹{(subtotal + delivery).toLocaleString('en-IN')}</span></div></div>{!checkout ? <Link href="/checkout" className="mt-6 flex min-h-12 w-full items-center justify-center gap-2 bg-primary text-sm font-bold text-primary-foreground hover:bg-secondary">Go to checkout <ArrowRight size={16} /></Link> : null}<p className="mt-4 flex items-center justify-center gap-2 text-xs text-muted-foreground"><ShieldCheck size={14} /> Secure checkout preview</p></div> }
export function CheckoutStepper({ active = 1 }: { active?: number }) { return <div className="flex items-center gap-2 overflow-x-auto border-y border-border py-4">{['Address', 'Delivery', 'Payment', 'Review'].map((step, index) => <div key={step} className="flex min-w-max items-center gap-2 text-xs font-bold uppercase tracking-wider"><span className={`grid size-7 place-items-center rounded-full border ${index + 1 <= active ? 'border-primary bg-primary text-primary-foreground' : 'border-border text-muted-foreground'}`}>{index + 1 < active ? <Check size={14} /> : index + 1}</span><span className={index + 1 === active ? 'text-foreground' : 'text-muted-foreground'}>{step}</span>{index < 3 ? <span className="mx-1 text-border">/</span> : null}</div>)}</div> }
